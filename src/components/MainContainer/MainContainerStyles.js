import styled from 'styled-components';
import { Segment } from 'semantic-ui-react';

const StyledSegment = styled(Segment)`
  min-height: 800px;
  max-height: 800px;
  padding: 1em 0em;
`;

export { StyledSegment };
